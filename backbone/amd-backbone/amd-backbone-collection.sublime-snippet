<snippet>
  <content>
    <![CDATA[
      define([
        'Backbone'${2:,
        '${3:Model}'}${4}
      ], function( Backbone${5:, ${6:Model}${7}} ){
        
        var ${1:collection} = Backbone.Collection.extend({

          ${8:model : ${9:Model}}

        });

        return ${1:collection};

      });
    ]]>
  </content>
  <tabTrigger>bb:amd:co</tabTrigger>
  <scope>source.js</scope>
  <description>Backbone AMD Collection</description>
</snippet>
